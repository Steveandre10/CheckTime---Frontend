/**
 * @file AppRoutes.jsx
 * @description Módulo reservado para la modularización de rutas del sistema.
 * Actualmente, para simplificar el flujo del proyecto, las rutas se encuentran definidas directamente en {@link App.jsx}.
 * Este archivo sirve de marcador de posición para futuras implementaciones de protectores de rutas (Route Guards), layouts compartidos o carga perezosa (Lazy Loading) de componentes.
 */

// Marcador de posición
export default {};
